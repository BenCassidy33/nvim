local M = {}

M.plugin = {
    "https://github.com/tjdevries/colorbuddy.nvim",
    config = function()
        -- return require("colorbuddy").setup(M.opts)
    end
}

M.opts = {
    
}

return M.plugin
